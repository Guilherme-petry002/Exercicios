void main(){

    int a = 105;
    float x = 101.31;
    printf("Com d: %d\n", a ); //Printa a varivel a;
    printf("Com 7d: %7d\n", a ); //Printa a variael a com 7 digitos vazios;
    printf("Com 07d: %07d\n", a ); //Printa a variavel a e preenche com 0 os espaços vazios;
    printf("Com -7d: %-7d\n", a ); //Printa a variavel a com - 7 digitos;
    printf("Com f: %f\n", a ); //Formata a variavel a de int para float;
    printf("Com 7f: %7f\n", x ); //Formata a variavel x para 7 digitos após a virgula;
    printf("Com .3f: %.3f\n", x); //Formata a variavel x para 3 digitos após a virgula.
    printf("Com 7.4: %7.4f\n", x); //Formata a variavel x para 4 digitos após a virgula.
    printf("Com c: %c\n", a); //Tranfosrma a em um char;
    printf("Com c: %c\n", x); // Tranforma x em um char;
    
    }
    